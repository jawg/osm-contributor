ALTER TABLE 'POI' RENAME to 'POI2';
CREATE TABLE `POI` (`BACKEND_ID` VARCHAR , `ID` INTEGER PRIMARY KEY AUTOINCREMENT , `LATITUDE` DOUBLE PRECISION NOT NULL , `LONGITUDE` DOUBLE PRECISION NOT NULL , `NAME` VARCHAR , `POI_TYPE_ID` BIGINT NOT NULL , `UPDATE_DATE` BIGINT , `UPDATED` SMALLINT NOT NULL , `VERSION` VARCHAR , `VISIBLE` SMALLINT , `WAY` SMALLINT NOT NULL , TO_DELETE SMALLINT NOT NULL);
INSERT INTO POI(BACKEND_ID, ID, LATITUDE, LONGITUDE, NAME, POI_TYPE_ID, UPDATE_DATE, UPDATED, VERSION, VISIBLE, WAY, TO_DELETE) SELECT BACKEND_ID, ID, LATITUDE, LONGITUDE, NAME, POI_TYPE_ID, UPDATE_DATE, UPDATED, VERSION, VISIBLE, WAY, 0 FROM POI2
DROP TABLE POI2