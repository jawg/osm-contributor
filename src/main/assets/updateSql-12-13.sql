CREATE TABLE `AREA` (`ID` INTEGER PRIMARY KEY AUTOINCREMENT, `NORTH` DOUBLE PRECISION NOT NULL, `SOUTH` DOUBLE PRECISION NOT NULL, `EAST` DOUBLE PRECISION NOT NULL, `WEST` DOUBLE PRECISION NOT NULL, `UPDATE_DATE` BIGINT);